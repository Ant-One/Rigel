- Planètes trop grandes
    Nn c b1

- Horizon penché à droite
    Zé bon

- Canvas qui ne request pas le focus et du coup les touches du clavier ne marche plus à moins de cliquer sur le canvas
    On peut pas vraiment changer ça non plus

- Object closest to ne devrait pas s'afficher s'il n'y en a pas
    OK controller si la distance est assez petite

- Temps pour jours qui change les heures

- Obligé d'ajouter les secondes, sinon le temps ne change pas. Hour picker JFX ?
    On peut rien changer

- Bindings.select ?